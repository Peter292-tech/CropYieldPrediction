import React from "react";
import "./Header.css";

const Header = () => {
  return (
    <header className="header">
      <h1>🌿 Welcome to Crop Yield Prediction System</h1>
      <p>Use this tool to estimate crop yield based on key agricultural factors.</p>
    </header>
  );
};

export default Header;